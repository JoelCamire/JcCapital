# Local preview screenshots (generated)
.preview_screenshots/

# Local Claude Code settings (per-developer, not part of the site)
.claude/

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
*~

# Node deps (only installed transiently for tests/screenshots)
node_modules/
app/package.json
app/package-lock.json

# Transient test/screenshot scripts at app root (real tests live in app/test/)
app/*.mjs

taste/
